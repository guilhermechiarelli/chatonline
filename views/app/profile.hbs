<div class="container">
  <div class="row mt-5">
    <div class="col-12">
      <center>
        <h1>Perfil de {{user.name}}</h1>
        <p>Suas informações estão descritas abaixo. Escolha a opção desejada.
        </p>
      </center>
    </div>
  </div>
  {{> _flash}}
  <div class="row justify-content-center">
    <div class="col-lg-6">
      <div class="form">
        <center>
          <form action="/app/profile/{{user._id}}" method="POST">
            <input type="text" name="name" value="{{user.name}}" class="form-control" maxlength="50" required>
            <input type="text" name="email" value="{{user.email}}" class="form-control mt-2" maxlength="50" required>
            <input type="text" name="city" value="{{user.city}}" class="form-control mt-2" maxlength="50" required>
            <input type="text" name="address" value="{{user.address}}" class="form-control mt-2" maxlength="50"
              required>
            <input type="text" name="cel" value="{{user.cel}}" class="form-control mt-2" maxlength="50" required>
            <input type="password" name="senha" placeholder="Digite sua senha" class="form-control mt-4" maxlength="30"
              required>
            <button type="submit" class="btn btn-primary mt-4">Alterar Usuário</button>
          </form>
          <form action="/app/profile/delete/{{user._id}}" method="POST"
            onsubmit="return confirm('Tem certeza que deseja DELETAR o usuário?')">
            <button type="submit" class="btn btn-danger mt-2 mb-2">Deletar Usuário</button>
          </form>
        </center>
        <a href="/app/chat">Voltar</a>
      </div>
    </div>
  </div>
  <center>

  </center>

</div>